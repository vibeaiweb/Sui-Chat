{
  "name": "sui-chat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run preview"
  }
}
